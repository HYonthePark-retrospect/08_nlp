# NLP 핵심 학습 노트

> 범위: 제공 자료의 NLP 개요, 전처리, 텍스트 벡터화, 임베딩, FastText, 기본 RNN
>
> **한 줄 흐름:** 원문 → 정제/정규화 → 토큰화 → ID화·패딩 → 벡터화/임베딩 → 모델 → 평가

## 0. 가장 먼저 구분할 5가지

| 용어 | 뜻 | 절대 혼동하지 말 것 |
|---|---|---|
| **토큰화** | 문자열을 처리 단위의 순서 있는 목록으로 나누기 | 숫자로 바꾸는 작업이 아니다 |
| **정규화** | 같은 뜻으로 볼 표기를 하나의 기준형으로 통일 | 예: `Theater → theater`, 연속 공백 → 한 칸 |
| **정제** | 분석에 불필요하거나 위험한 정보를 제거·마스킹 | 예: URL 제거, 이메일 마스킹, 불용어 제거 |
| **정수 인코딩** | 각 토큰에 사전상의 정수 ID를 부여 | ID 크기에 의미·거리 정보는 없다 |
| **벡터화** | 토큰/문서/문장을 숫자 배열로 표현 | BoW·TF-IDF·원-핫·임베딩이 모두 포함된다 |

**암기:** *나눈다=토큰화, 맞춘다=정규화, 뺀다=정제, 번호=인코딩, 숫자 배열=벡터화.*

## 1. 기본 단위와 전처리

### 필수 정의

- **코퍼스(Corpus)**: NLP 분석·학습에 쓰는 텍스트 집합.
- **어휘 사전(Vocabulary, V)**: 모델이 아는 고유 토큰의 집합.
- **형태소(Morpheme)**: 의미 또는 문법 기능을 지닌 최소 단위. 한국어는 조사·어미 때문에 어절 토큰화만으로 부족할 수 있다.
- **OOV (Out Of Vocabulary)**: 현재 사전에 없는 토큰.
- **불용어(Stopword)**: 현재 과업에서 정보량이 낮아 제외하기로 한 단어. 항상 제거하는 것이 아니라 과업에 따라 결정한다.

### 토큰화 단위 선택

| 단위 | 장점 | 주의점/용도 |
|---|---|---|
| 문장 | 문서 구조 보존 | 문장 분류·요약의 첫 단계 |
| 단어/어절 | 읽기 쉽고 직관적 | 한국어 조사·어미가 붙음 |
| 형태소 | 한국어 문법 단위 반영 | 분석기별 결과가 다름 |
| 서브워드 | OOV를 줄임 | 조각이 형태소와 일치하지 않음 |
| 문자 | 모든 문자열 처리 가능 | 시퀀스가 길어짐 |

**WordPiece:** 자주 쓰이는 문자열 조각을 학습한 사전으로 토큰화한다. `##ing`의 `##`는 앞 조각에 이어지는 단어 내부 조각이라는 표시다. BERT에서는 `[CLS]`(분류용 시작), `[SEP]`(문장 경계)를 쓴다.

### 정제·정규화의 실전 원칙

1. 원문을 보존한다.
2. 유니코드·공백·도메인 표기를 정규화한다.
3. 이메일·URL 등 민감/잡음 정보를 마스킹한다.
4. 토큰화한다.
5. 과업에 맞춰 불용어·희귀어를 필터링한다.
6. 전후의 어휘 수, OOV 비율, 성능을 비교한다.

감성 분류에서는 `not`, `no`, `않다` 같은 **부정어를 불용어로 삭제하면 안 된다.**

### 어간 추출 vs 표제어 추출

| 방법 | 정의 | 예 | 특징 |
|---|---|---|---|
| **Stemming** | 규칙으로 접사를 잘라 어간 비슷한 형태를 만듦 | `running → run`, 때로는 부자연스러운 형태 | 빠르지만 의미·문법을 덜 고려 |
| **Lemmatization** | 품사·사전을 고려해 사전 표제어로 변환 | `ran → run`, `better → good` | 더 자연스럽지만 분석 비용 큼 |

한국어는 단순히 끝을 자르기보다 형태소 분석기를 쓴다. Okt의 `stem=True`는 문법적 어간만이 아니라 `기다리고 → 기다리다`처럼 **기본형에 가까운 형태**를 준다.

## 2. 정규표현식(RegEx) 최소 암기표

| 패턴 | 뜻 | 예 |
|---|---|---|
| `.` | 임의의 한 문자 | `a.c` → `abc`, `a9c` |
| `?` | 앞 요소 0 또는 1회 | `colou?r` |
| `*` | 앞 요소 0회 이상 | `ab*` |
| `+` | 앞 요소 1회 이상 | `\d+` |
| `{m,n}` | m~n회 반복 | `\d{2,4}` |
| `[]` | 문자 집합 중 하나 | `[A-Za-z]` |
| `\d`, `\w`, `\s` | 숫자, 단어 문자, 공백 | `\d{3}-\d{4}` |
| `^`, `$` | 문자열 시작, 끝 | `^Hello$` |
| `|` | 또는(OR) | `cat|dog` |
| `()` | 그룹/캡처 | `(ab)+` |

**탐욕성:** `.*`는 가능한 길게 잡는다. 필요한 경우 `.*?`처럼 최소 매칭을 쓴다.

## 3. ID화, 특수 토큰, 패딩

### 정수 인코딩

토큰 사전이 `{PAD:0, UNK:1, i:2, love:3, nlp:4}`일 때 `i love new`는 `[2, 3, 1]`이다.

- **`<PAD>`**: 배치 안의 문장 길이를 같게 채우는 토큰. 보통 ID 0.
- **`<UNK>`**: 사전에 없는 단어를 대신하는 토큰.
- **padding**: 짧은 시퀀스에 PAD를 추가해 길이를 맞춤.
- **truncation**: 너무 긴 시퀀스를 최대 길이에서 자름.

`pre` 패딩/자르기는 앞쪽, `post`는 뒤쪽을 기준으로 한다. RNN이 마지막 상태를 쓰는 구조에서는 의미 있는 마지막 단어를 보존하도록 선택해야 한다.

## 4. 희소 벡터: 원-핫, BoW, DTM, TF-IDF

### 원-핫 인코딩

어휘 크기가 \(|V|\)이면 한 토큰은 길이 \(|V|\) 벡터이며, 자기 ID 위치만 1이고 나머지는 0이다.

\[
\mathbf{o}_i=[0,\ldots,0,1,0,\ldots,0]
\]

단어가 서로 다른 칸에 있을 뿐이므로 단어 의미 유사도를 표현하지 못하고, 사전이 커질수록 대부분 0인 **희소 벡터**가 된다.

### BoW와 DTM

- **BoW (Bag of Words)**: 문서 내 단어별 등장 횟수 벡터. 기본 BoW는 순서를 버린다.
- **DTM (Document-Term Matrix)**: 행=문서, 열=단어, 값=문서의 단어 빈도인 행렬.
- **TDM (Term-Document Matrix)**: DTM을 전치한 것.
- **N-gram**: 연속한 N개 토큰. unigram(1), bigram(2), trigram(3). 일부 어순을 보존하지만 특성 수가 증가한다.

### TF-IDF: 가장 중요한 공식

문서 \(d\), 단어 \(t\), 전체 문서 수 \(N\), 단어 \(t\)를 포함한 문서 수 \(df(t)\)라고 하자.

\[
TF(t,d)=\text{문서 }d\text{에서 }t\text{의 빈도}
\]
\[
IDF(t)=\log\frac{N}{df(t)}
\]
\[
TF\text{-}IDF(t,d)=TF(t,d)\times IDF(t)
\]

직관은 **“이 문서에서 자주 나오되, 다른 문서 전체에는 드문 단어”에 큰 점수**다.

- 모든 문서에 나오는 단어: \(df(t)\approx N\) → \(IDF\approx0\) → 중요도 낮음
- 한 문서에만 나오는 단어: \(df(t)\) 작음 → IDF 큼 → 구별력 큼

실무 라이브러리는 0 나누기 방지와 안정성을 위해 smoothing을 넣을 수 있다. 핵심 원리는 변하지 않는다.

### 코사인 유사도

두 문서/단어 벡터 \(\mathbf{a},\mathbf{b}\)의 방향 유사도:

\[
\cos(\theta)=\frac{\mathbf{a}\cdot\mathbf{b}}{\|\mathbf{a}\|\,\|\mathbf{b}\|}
\]

- 1: 방향 동일, 0: 관련 약함(직교), -1: 반대 방향
- BoW/TF-IDF에서는 문서 길이 차이를 어느 정도 덜 받으면서 단어 구성의 유사도를 비교한다.

## 5. 밀집 벡터: 임베딩

### 임베딩의 핵심

임베딩 행렬 \(E\in\mathbb{R}^{|V|\times d}\)에서 토큰 ID \(i\)의 **i번째 행**을 꺼낸다.

\[
\mathbf{e}_i=E[i] \quad (\mathbf{e}_i\in\mathbb{R}^d)
\]

원-핫 \(\mathbf{o}_i\)를 쓰면 같은 뜻을 \(\mathbf{o}_i^TE\)로 표현할 수 있다. 즉, 임베딩은 원-핫과 행렬 곱의 결과이지만 실제 구현은 훨씬 효율적인 **행 조회(lookup)** 다.

입력 shape이 `(B, L)`이고 임베딩 차원이 `d`이면 출력은 `(B, L, d)`다.

### Word2Vec

**분포 가설:** 비슷한 문맥에서 등장하는 단어는 비슷한 의미를 가진다.

| 방식 | 무엇을 예측? | 암기 |
|---|---|---|
| **CBOW** | 주변 단어 → 중심 단어 | 주변으로 중심을 맞힌다 |
| **Skip-gram** | 중심 단어 → 주변 단어 | 중심에서 주변을 뿌린다 |

Word2Vec은 단어 전체를 사전 단위로 다루므로 사전에 없는 새 단어에는 벡터가 없다(OOV).

### FastText

단어 자체뿐 아니라 문자 n-gram을 함께 학습한다. 예를 들어 `<apple>`의 3-gram은 `<ap`, `app`, `ppl`, `ple`, `le>`이다.

따라서 `apples`가 미등록 OOV여도 `apple`과 공유한 문자 조각의 벡터를 조합해 표현을 만들 수 있다. 다만 **벡터를 만들 수 있음 ≠ 의미를 정확히 안다.** 철자가 비슷하지만 의미가 다른 단어에는 취약할 수 있다.

### 모델 선택 빠른 기준

| 상황 | 우선 고려 |
|---|---|
| 빠른 문서 분류, 해석 가능한 중요 단어 | TF-IDF + 선형 모델 |
| 단어 유사도·의미 관계 | Word2Vec/사전학습 임베딩 |
| 오탈자·희귀어·형태 변화가 많음 | FastText |
| 문맥에 따라 단어 뜻이 달라짐 | BERT류 문맥 임베딩 |

## 6. PAD와 평균 임베딩의 마스킹

PAD까지 포함해 단순 평균을 내면 짧은 문장이 불리해진다. 실제 토큰 위치는 1, PAD는 0인 마스크 \(m_t\)를 사용한다.

\[
\text{sentence vector}=
\frac{\sum_{t=1}^{L}m_t\mathbf{e}_t}{\sum_{t=1}^{L}m_t}
\]

분자와 분모에서 모두 PAD를 제외한다는 것이 핵심이다. PyTorch `nn.Embedding(..., padding_idx=0)`에서는 PAD 행을 0 벡터로 유지할 수 있지만, **분모 보정(마스크 평균)은 여전히 필요**하다.

## 7. 순차 데이터와 기본 RNN

### 정의와 수식

**순차 데이터**는 위치와 순서가 의미를 바꾸는 데이터다. NLP에서는 문장=시퀀스, 각 토큰 위치=타임스텝이다.

RNN은 현재 입력 \(x_t\)와 이전 요약 \(h_{t-1}\)로 새 은닉 상태를 만든다.

\[
h_t=\tanh(W_{xh}x_t+W_{hh}h_{t-1}+b_h)
\]

\[
y_t=W_{hy}h_t+b_y
\]

- \(x_t\): t번째 토큰의 입력 벡터
- \(h_{t-1}\): 이전 시점까지 읽은 내용의 요약(은닉 상태)
- \(h_t\): 현재까지의 새 요약
- 같은 가중치 \(W\)를 모든 시점에 공유한다.

**암기:** *RNN은 현재 입력 + 과거 기억을 반복한다.*

### Tensor shape

`batch_first=True`일 때:

- 입력: `(B, L, F)` = 배치 크기, 시퀀스 길이, 입력 특성 수
- `output`: `(B, L, H)` = 모든 시점의 은닉 상태
- `hidden`: `(num_layers × directions, B, H)` = 각 층의 마지막 은닉 상태

한 층·단방향 RNN에서는 `output[:, -1, :] == hidden[-1]`이다. 시퀀스 분류는 보통 이 마지막 은닉 상태를 선형층에 넣어 문장별 로짓을 만든다.

### RNN의 한계

긴 시퀀스를 거꾸로 학습할 때 기울기가 반복 곱해져 매우 작아지는 **기울기 소실** 또는 커지는 **기울기 폭주**가 생길 수 있다.

- `gradient clipping`: 큰 기울기의 norm을 제한해 폭주를 완화한다.
- 하지만 사라진 작은 기울기를 복원하지는 못한다.
- LSTM/GRU는 게이트 구조로 장기 의존성 문제를 더 잘 다루도록 설계됐다.

## 8. 시험 직전 12문장

1. 토큰화는 문자열을 토큰 목록으로 나누는 일이고, 정수 인코딩은 토큰에 번호를 매기는 일이다.
2. 정규화는 표기를 맞추고, 정제는 정보를 제거하거나 가린다.
3. OOV는 현재 어휘 사전에 없는 토큰이다.
4. PAD는 길이를 맞추기 위한 자리표시자이고, UNK는 모르는 단어의 대체 ID다.
5. 원-핫은 구분은 잘하지만 의미 관계를 표현하지 못하는 희소 벡터다.
6. BoW는 단어 빈도를 세므로 기본적으로 단어 순서를 잃는다.
7. TF-IDF는 한 문서에는 많고 전체에는 드문 단어에 높은 가중치를 준다.
8. 코사인 유사도는 벡터의 크기보다 방향의 유사도를 본다.
9. 임베딩은 토큰 ID로 임베딩 행렬의 한 행을 조회한 밀집 벡터다.
10. Word2Vec은 문맥으로 의미를 학습하지만 OOV 단어를 직접 표현하지 못한다.
11. FastText는 문자 n-gram을 조합해 OOV 벡터를 만들 수 있다.
12. RNN은 현재 입력과 이전 은닉 상태를 함께 사용하며, 긴 문장에서는 기울기 소실 문제가 있다.

## 9. 스스로 점검하는 계산 문제

1. 문서가 100개이고 단어 A가 100개 문서, 단어 B가 2개 문서에 등장한다. 어느 단어의 IDF가 큰가? **B.**
2. 어휘 크기가 20,000이면 원-핫 벡터 길이는? **20,000.**
3. 입력이 `(32, 50)`이고 임베딩 차원이 128이면 임베딩 출력 shape는? **`(32, 50, 128)`.**
4. RNN 입력 `(16, 30, 64)`, hidden size 128, 한 층 단방향의 `output` shape는? **`(16, 30, 128)`.**
5. FastText가 OOV 단어의 벡터를 만들 수 있는 근거는? **학습된 문자 n-gram의 조합.**
